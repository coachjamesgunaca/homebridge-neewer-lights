/**
 * Platform name registered with Homebridge. Must match the `platform` field in
 * the Homebridge config and `pluginAlias` in config.schema.json.
 */
export const PLATFORM_NAME = 'NeewerLightsPlatform';

/**
 * Must match the package.json `name`.
 */
export const PLUGIN_NAME = 'homebridge-neewer-lights';
